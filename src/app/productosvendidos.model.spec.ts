import { Productosvendidos } from './productosvendidos.model';

describe('Productosvendidos', () => {
  it('should create an instance', () => {
    expect(new Productosvendidos()).toBeTruthy();
  });
});
